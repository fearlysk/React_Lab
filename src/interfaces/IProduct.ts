export default interface IProduct {
  id: number;
  image: string;
  title: string;
  price: number;
  genre: string;
  rating: number;
  pc: boolean;
  xbox: boolean;
  playstation: boolean;
  description: string;
}
