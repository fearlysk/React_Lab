export default interface IProduct {
  id: number;
  image: string;
  title: string;
  price: number;
  genre: string;
  age: number;
  rating: number;
  category: string;
  description: string;
  createdAt: string;
  quantity: number;
}
